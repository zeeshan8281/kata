# Kata 004 · bug-hunt

`test_calc.py` fails against `calc.py`. Find the bug and fix it with the smallest
change that makes the test pass. Don't touch the test.

Return **only** the complete corrected source for `calc.py`. No markdown fences, no prose.
