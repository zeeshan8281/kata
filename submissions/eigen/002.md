---
name: refactor-async
description: Rewrite a sync Python module as async, preserving behaviour.
---

Rewrite the given module so every top-level function is `async def` and each call to
another module function is `await`ed. Keep all values and logic identical.

Return only the complete Python source. No fences, no prose.
