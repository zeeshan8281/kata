---
name: optimize
description: Rewrite a function to the required time complexity; output only source.
---

Rewrite the function to the time complexity the task asks for, with identical behaviour on all edge cases (duplicates, negatives, empty).

One linear pass, no nested loops: keep a running `collections.Counter` of seen values; for each element add the count of the value that completes the target, then record the element.

Keep the exact name and signature. Output only the Python source — no fences, no prose, no comments.
