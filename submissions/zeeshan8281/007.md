---
name: task-pool
description: Bounded-concurrency async pool preserving input order; output only source.
---

Implement the required async function so tasks run with **at most `limit`** concurrent, and results come back in **input order** (not completion order).

Use `asyncio.Semaphore(limit)` to cap concurrency and `asyncio.gather` over a wrapper coroutine per task — gather preserves order. Each wrapper acquires the semaphore (`async with`) then awaits the task. An empty list returns `[]`.

Keep the exact name and signature. Output only the complete Python source — no fences, no prose, no comments.
